# download ctags (taglist vim plugin dependency)
sudo apt-get install ctags
echo "ctags installed"

# set vim as default editor
sudo ln -sf /usr/bin/vim.basic /etc/alternatives/editor 
echo "vim set as default editor"
